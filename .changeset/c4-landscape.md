---
'mermaid': minor
---

C4 (legacy) diagrams now support a `C4Landscape` diagram type. A System
Landscape is a Level-1 view of all people and software systems across the
enterprise. It accepts the same statements as `C4Context`
(person/system/boundary/rel) and renders through the same unified pipeline;
it only declares a different diagram type.
